import { {{pascalCase moduleName}} } from "modules/{{lowerCase moduleName}}/entity/{{camelCase moduleName}}.entity";
export const {{camelCase moduleName}}Repository = AppDataSource.getRepository({{pascalCase moduleName}});